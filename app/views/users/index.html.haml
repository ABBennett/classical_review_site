%ul
  - @users.each do |user|
    %li= link_to "#{user.username}", user_path(user)
    - if current_user.admin
      %li= link_to "Delete #{user.username}", user_path(user), method: :delete, confirm: "Are You Sure?"
