= form_for(@piece) do |f|
  %div
    = f.label :title
    = f.text_field :title, placeholder: "Title"

  %div
    = f.label :composer
    = f.text_field :composer, placeholder: "Composer"

  .actions
    = f.submit "Add Piece"
